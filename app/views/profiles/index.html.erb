<h1>Profile#index</h1>
<p>Find me in app/views/profile/index.html.erb</p>

 

<%=link_to Profile.find_by_id(1).lname, Profile.find_by_id(1) %>

<% User.all.each do |w|%> 
			<%if w.profile%>
			<%=link_to w.email, profile_path(w.profile.id) %>

			<%end%><%end%>